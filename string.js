// String is a sequence of character
let str = 'pepcoder'
//length of string 
console.log(str.length)

// methods 
//1.Slice(start, end+1)
let sliceStr = str.slice(3,8)
console.log(sliceStr)

// 2. subStr(startIdx, length)
let subStr = str.substring(3,4)
console.log(subStr)

//3. replace(replaceBy, replaceWith)

//4. ToUpperCase and ToLowerCase
// str.ToUpperCase()

//5. CANCATENATION
//  FIRSTSTRING.CONCATE(SECONDSTRING)

//6. Trim()
